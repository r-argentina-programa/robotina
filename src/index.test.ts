describe('Demo tests for github actions', () => {
  it('1 + 2 should be equal to 3', () => {
    const EXPECTED_RESPONSE = 3
    const test = 1 + 2
    expect(test).toEqual(EXPECTED_RESPONSE)
  })
})
